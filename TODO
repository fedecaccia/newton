install:
	get MPI_DIR, PETS_DIR & stop with message if they are empty
	create folder and links
	hostname and create mf files
	make all
tests should be compiled with preprocessor flags and different names
