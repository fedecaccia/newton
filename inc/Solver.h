#ifndef SOLVER_H
#define SOLVER_H

class Solver
{
	public:
		// Functions
		Solver();
		// Initialization
		int initialize();
		// Residual calculation
		int run();
		// Finalization
		int finish();
		
		
	private:

};

#endif